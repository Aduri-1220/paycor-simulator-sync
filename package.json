{
  "name": "paycor-simulator-sync",
  "version": "1.0.0",
  "description": "Sync employee data from CSV/JSON (System A) to SQLite payroll (System B)",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "sync": "node src/index.js once",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.9.0",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "vitest": "^3.1.4"
  }
}
